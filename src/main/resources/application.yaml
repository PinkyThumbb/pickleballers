spring:
  application:
    name: pickleballers
  datasource:
    url: jdbc:postgresql://localhost:5432/pickleball
    username: pickle
    password: ball
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
